pipeline {
  agent any 
  stages {
    stage('Install') {
      steps {
        script {
          sh 'npm i'
          sh 'npx lerna bootstrap'
        }
      }
    }
    stage('Trigger') {
      steps {
        build job: "test-job", wait: true
      }
    }
  }
}